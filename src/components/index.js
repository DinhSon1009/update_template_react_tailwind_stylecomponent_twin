import React from "react";
import { StyledButton } from "../styles/StyledButton"; // new

const ButtonComponent = () => {
  return <StyledButton isSecondary>Click Me!</StyledButton>;
};

export default ButtonComponent;
